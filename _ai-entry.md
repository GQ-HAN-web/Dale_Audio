# AI Entry

This is the AI entry file for `D:\Obsidian_Zhagen_Vault`. When Claude connects to the GitHub mirror, read this file first, then use the index layer, then open original notes only when needed.

## What This Vault Contains

This Obsidian vault is centered on `扎根`. It contains audio transcripts, Q&A records, opening/closing sessions, student dialogues, daily Q&A, and a small set of core concept notes.

Current readable Markdown scale:

- Transcripts: 125
- Concept notes: 7
- Tooling prompts: 15
- AI navigation files: `_ai/`

## Reading Order For AI

1. Read `_ai-entry.md` first.
2. Read `_hot.md` for the current high-value entry points.
3. Read `_index.md` for the full vault map.
4. Use `_ai/file-summaries.md` for human-readable file summaries.
5. Use `_ai/index.jsonl` for machine-readable retrieval.
6. Open only the 5-20 original notes most relevant to the question.

## Rules

- Do not attempt to read the whole vault at once.
- Before answering, state which files you actually used.
- For a concrete topic, search `_ai/index.jsonl`, `_ai/file-summaries.md`, filenames, tags, and wiki links before opening source notes.
- If the question involves `扎根`, `答疑`, `开营`, `结营`, or student dialogue, prefer date and topic from filenames.
- `.obsidian/` is app configuration and `.smart-env/` is plugin/vector cache. They are not primary knowledge content.
- `_ai/` is the navigation layer for AI. Original Markdown notes remain the source of truth.

## Main Entry Points

- [[_hot]]: high-value and recent entry points
- [[_index]]: full vault index
- [[_ai/file-summaries]]: per-file summaries
- [[_ai/topic-map]]: topic map
- `_ai/index.jsonl`: machine-readable index

## Suggested Prompt Pattern

- "Read `_ai-entry.md` and `_index.md` first, then answer..."
- "Search `_ai/index.jsonl` for topic X and open only the most relevant original notes."
- "Use `_ai/file-summaries.md` to choose 10 notes that deserve deep reading."
- "Do not assume you have read the whole vault; list the files you actually read."

## Current Structure

- `20xxxxxx_...扎根...????.md`: dated transcripts of sessions, Q&A, openings, closings, and dialogues.
- `发愿.md`, `实相.md`, `我执.md`, `爱.md`, `精进.md`, `臣服.md`, `觉知.md`: core concept notes.
- `_relationship_graph.md`: existing relationship/index material.
- `copilot/`: Obsidian Copilot prompt templates, not the main knowledge layer.
